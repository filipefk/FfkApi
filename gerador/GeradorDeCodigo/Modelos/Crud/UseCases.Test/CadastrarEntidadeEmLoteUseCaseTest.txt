<#
{
	"variaveis": [
		{
			"nome": "{{Entidade}}",
			"substituirPor": "NomeEntidade",
			"modificador": null
		},
		{
			"nome": "{{entidade}}",
			"substituirPor": "NomeEntidade",
			"modificador": "Variavel"
		},
		{
			"nome": "{{ENTIDADE}}",
			"substituirPor": "NomeEntidade",
			"modificador": "ToUpper"
		},
		{
			"nome": "{{Entidades}}",
			"substituirPor": "NomeEntidadePlural",
			"modificador": null
		},
		{
			"nome": "{{PastaTestes}}",
			"substituirPor": "PastaTestes",
			"modificador": null
		}
	],
	"arquivoDestino": "{{PastaTestes}}\\UseCases.Test\\{{Entidade}}\\CadastrarEmLote\\Cadastrar{{Entidade}}EmLoteUseCaseTest.cs"
}
#>

using FfkApi.Application.UseCases.{{Entidade}}.CadastrarEmLote;
using FfkApi.Communication.Requests;
using FfkApi.Domain.Enums;
using FfkApi.Exceptions;
using NUnit.Framework;
using TestUtil.AutoMapper;
using TestUtil.Entities;
using TestUtil.Repositories;
using TestUtil.Requests;

namespace UseCases.Test.{{Entidade}}.CadastrarEmLote;

[TestFixture]
[Parallelizable(ParallelScope.All)]
public class Cadastrar{{Entidade}}EmLoteUseCaseTest
{
    [Test]
    public async Task Sucesso_Total()
    {
        var cancellationToken = new CancellationTokenSource().Token;

        var request = RequestCadastrar{{Entidade}}EmLoteBuilder.Build();

        var useCase = CriarUseCase(cancellationToken: cancellationToken);

        var response = await useCase.Execute(request, cancellationToken);

        Assert.That(response, Is.Not.Null);
        Assert.That(response.Cadastrados, Is.Not.Null);
        Assert.That(response.Cadastrados.Count, Is.EqualTo(request.{{Entidades}}.Count));

        // TODO : [GERADOR DE CÓDIGO] => FAZER O ASSERT DOS OUTROS CAMPOS DA ENTIDADE AQUI

        Assert.That(response.Erros, Is.Not.Null);
        Assert.That(response.Erros, Is.Empty);

        Assert.That(response.TotalCadastrados, Is.Not.Null);
        Assert.That(response.TotalCadastrados, Is.EqualTo(request.{{Entidades}}.Count));

        Assert.That(response.TotalErros, Is.Not.Null);
        Assert.That(response.TotalErros, Is.EqualTo(0));

        Assert.That(response.Resultado, Is.EqualTo(StatusCadastroLote.SucessoTotal.ToString()));
    }

    [Test]
    public async Task Sucesso_Parcial()
    {
        var cancellationToken = new CancellationTokenSource().Token;

        var request = RequestCadastrar{{Entidade}}EmLoteBuilder.Build();
        var {{entidade}} = request.{{Entidades}}.FirstOrDefault();
        // TODO : [GERADOR DE CÓDIGO] => COLOCAR ALGO QUE GERE UM ERRO AQUI

        var useCase = CriarUseCase(cancellationToken: cancellationToken);

        var response = await useCase.Execute(request, cancellationToken);

        Assert.That(response, Is.Not.Null);
        Assert.That(response.Cadastrados, Is.Not.Null);
        Assert.That(response.Cadastrados.Count, Is.EqualTo(request.{{Entidades}}.Count - 1));

        // TODO : [GERADOR DE CÓDIGO] => FAZER O ASSERT DOS OUTROS CAMPOS DA ENTIDADE AQUI

        Assert.That(response.Erros, Is.Not.Null);
        Assert.That(response.Erros.Count, Is.EqualTo(1));

        Assert.That(response.TotalCadastrados, Is.Not.Null);
        Assert.That(response.TotalCadastrados, Is.EqualTo(request.{{Entidades}}.Count - 1));

        Assert.That(response.TotalErros, Is.Not.Null);
        Assert.That(response.TotalErros, Is.EqualTo(1));

        Assert.That(response.Resultado, Is.EqualTo(StatusCadastroLote.SucessoParcial.ToString()));
    }

    [Test]
    public async Task Falha_Total()
    {
        var cancellationToken = new CancellationTokenSource().Token;

        var request = RequestCadastrar{{Entidade}}EmLoteBuilder.Build(2);
        var {{entidade}}1 = request.{{Entidades}}.FirstOrDefault();
        // TODO : [GERADOR DE CÓDIGO] => COLOCAR ALGO QUE GERE UM ERRO AQUI

        var {{entidade}}2 = request.{{Entidades}}.Skip(1).FirstOrDefault();
        // TODO : [GERADOR DE CÓDIGO] => COLOCAR ALGO QUE GERE UM ERRO AQUI

        var useCase = CriarUseCase(cancellationToken: cancellationToken);

        var response = await useCase.Execute(request, cancellationToken);

        Assert.That(response, Is.Not.Null);
        Assert.That(response.Cadastrados, Is.Not.Null);
        Assert.That(response.Cadastrados, Is.Empty);

        Assert.That(response.Erros, Is.Not.Null);
        Assert.That(response.Erros.Count, Is.EqualTo(request.{{Entidades}}.Count));

        // TODO : [GERADOR DE CÓDIGO] => FAZER O ASSERT DOS OUTROS CAMPOS DA ENTIDADE AQUI

        Assert.That(response.TotalCadastrados, Is.Not.Null);
        Assert.That(response.TotalCadastrados, Is.EqualTo(0));

        Assert.That(response.TotalErros, Is.Not.Null);
        Assert.That(response.TotalErros, Is.EqualTo(request.{{Entidades}}.Count));

        Assert.That(response.Resultado, Is.EqualTo(StatusCadastroLote.Falha.ToString()));
    }

    [Test]
    public async Task Erro_Lista_{{Entidades}}_Vazia()
    {
        var cancellationToken = new CancellationTokenSource().Token;

        var request = new RequestCadastrar{{Entidade}}EmLote
        {
            {{Entidades}} = []
        };

        var useCase = CriarUseCase(cancellationToken: cancellationToken);

        var response = await useCase.Execute(request, cancellationToken);

        Assert.That(response, Is.Not.Null);
        Assert.That(response.Cadastrados, Is.Not.Null);
        Assert.That(response.Cadastrados, Is.Empty);

        Assert.That(response.Erros, Is.Not.Null);
        Assert.That(response.Erros.Count, Is.EqualTo(1));
        Assert.That(response.Erros.FirstOrDefault()!.MensagensDeErro.Count, Is.EqualTo(1));
        Assert.That(response.Erros.FirstOrDefault()!.MensagensDeErro, Contains.Item(ResourceMessagesException.LISTA_DE_{{ENTIDADE}}_VAZIA));
        Assert.That(response.Erros.FirstOrDefault()!.Request, Is.Null);
    }


    private static Cadastrar{{Entidade}}EmLoteUseCase CriarUseCase(
        CancellationToken cancellationToken)
    {
        var {{entidade}}Repository = new {{Entidade}}RepositoryBuilder();

        // TODO : [GERADOR DE CÓDIGO] => PREPARAR OS SETUPS DO REPOSITÓRIO AQUI

        return new Cadastrar{{Entidade}}EmLoteUseCase(
            {{entidade}}Repository.Build(),
            UnitOfWorkBuilder.Build(),
            MapperBuilder.Build());

    }
}
